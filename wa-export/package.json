{
  "name": "wa-export",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated WhatsApp chat-history export service for Nehgz client onboarding. Runs standalone (own port + session dir + Chromium) — never touches the shared openwa-api container.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "qrcode": "^1.5.4",
    "whatsapp-web.js": "1.34.7"
  }
}
