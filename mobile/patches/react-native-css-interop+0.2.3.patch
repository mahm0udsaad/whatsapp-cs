diff --git a/node_modules/react-native-css-interop/babel.js b/node_modules/react-native-css-interop/babel.js
index d84e52b..2044df2 100644
--- a/node_modules/react-native-css-interop/babel.js
+++ b/node_modules/react-native-css-interop/babel.js
@@ -1,3 +1,9 @@
+// Patched via patch-package (patches/react-native-css-interop+0.2.3.patch).
+// Upstream unconditionally adds "react-native-worklets/plugin" here. That
+// duplicates the worklets/reanimated plugin that babel-preset-expo (SDK 54)
+// already injects automatically and last. Running the worklets plugin more
+// than once breaks the NativeWind JSX transform (className stops applying),
+// so we DO NOT add it here — babel-preset-expo owns it.
 module.exports = function () {
   return {
     plugins: [
@@ -9,8 +15,6 @@ module.exports = function () {
           importSource: "react-native-css-interop",
         },
       ],
-      // Use this plugin in reanimated 4 and later
-      "react-native-worklets/plugin",
     ],
   };
 };
